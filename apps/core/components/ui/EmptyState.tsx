/**
 * The standard "nothing here yet" state, re-exported at the path this app has always imported it
 * from. It lives in `@assistant-hub/ui` so app-contributed dashboard pages are
 * built from the same primitives as the shell's own (PLAN.md, "Dashboard
 * composition").
 */
export { EmptyState } from "@assistant-hub/ui";
