{
  "name": "@assistant-hub/tg-ui",
  "version": "0.0.0",
  "private": true,
  "description": "The tg source app's dashboard UI subpackage: typed extensions the core shell composes at build time (PLAN.md, \"Dashboard composition\"). Imports shared packages only — never the app's server code.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@assistant-hub/contracts": "*",
    "@assistant-hub/ui": "*"
  },
  "peerDependencies": {
    "react": "^19"
  },
  "devDependencies": {
    "@types/react": "^19",
    "typescript": "^5"
  }
}
