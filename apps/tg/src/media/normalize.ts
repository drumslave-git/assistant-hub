/**
 * Image normalization, re-exported at the path this app has always imported
 * it from — the implementation is shared (`@assistant-hub-swarm/media`).
 */
export { VISION_MAX_DIMENSION, normalizeImageForChat } from "@assistant-hub-swarm/media";
