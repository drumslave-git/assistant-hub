{
  "name": "llm-tg-bot-nextjs",
  "version": "1.48.0",
  "private": true,
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "npm run start --workspace @assistant-hub/core",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:watch": "npm run test:watch --workspace @assistant-hub/core",
    "test:integration": "turbo run test:integration",
    "test:linux": "docker compose -f docker-compose.test.yml run --rm test",
    "db:generate": "npm run db:generate --workspace @assistant-hub/core",
    "db:migrate": "npm run db:migrate --workspace @assistant-hub/core",
    "db:studio": "npm run db:studio --workspace @assistant-hub/core",
    "release:patch": "npm version patch --no-git-tag-version",
    "release:minor": "npm version minor --no-git-tag-version",
    "release:major": "npm version major --no-git-tag-version"
  },
  "devDependencies": {
    "turbo": "^2.5.8"
  }
}
