{
  "name": "@assistant-hub/db",
  "version": "0.0.0",
  "private": true,
  "description": "Shared database tooling: pg pool singletons, drizzle helpers, and the production migration runner. Each app defines its OWN schema and migration chain on top of this.",
  "exports": {
    ".": "./src/index.ts",
    "./import": "./src/import.ts",
    "./testing": "./src/testing.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "pg": "^8.22.0",
    "server-only": "^0.0.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/pg": "^8.20.0",
    "typescript": "^5",
    "@testcontainers/postgresql": "^12.0.4"
  }
}
