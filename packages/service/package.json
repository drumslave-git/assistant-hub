{
  "name": "@assistant-hub-swarm/service",
  "version": "0.0.0",
  "private": true,
  "description": "What every source-app service needs and none of them should own twice: bootstrap env access, the internal-token guard on their HTTP surface, serving their own MCP server over Hono, and the source-parameterized bus helpers (trace client, dashboard refresh ping).",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@assistant-hub-swarm/contracts": "*",
    "@hono/node-server": "^1.14.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "^4.1.10"
  }
}
