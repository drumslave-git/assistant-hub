{
  "name": "@assistant-hub/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Zod schemas shared across apps: the source-app contract (events, context provider, listing/CRUD API shapes), queue payloads, bus events, scoped entity refs, API DTOs.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
